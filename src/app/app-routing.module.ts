/**
 * Deprecated routing module stub.
 * Standalone routing is configured via `src/app/app.routes.ts` and `provideRouter()` in `src/main.ts`.
 * This file is intentionally left as a no-op to avoid conflicts.
 */
export class AppRoutingModule {}
